# simple program that asks the user for two numbers
# divides the first with the second than prints the result on screen

# user input two numbers
num1 = float(input('Input the first number: '))
num2 = float(input('Input the second number: '))
# run operation, than print result on screen
print('The division of the first number and the second number is:', num1 / num2 )
